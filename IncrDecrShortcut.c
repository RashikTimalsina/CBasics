#include <stdio.h>

int main(){
	
	//augumented assignment operators=used to replace a statement where an operator 
	//									takes a variaable as one of its arguments
	//									and then assigns the result back to the same variables
	//									x=x+1;
	//									X+=1;
	
	
	int x=10;
	
	
	 //x=x+2;
	 //x+=2;					//either of these two methods can be used as increment 
	
	// x=x-3;
	// x-=3;`					//either of these two methods can be used as decrement 
	
	
	// x=x*4;
	// x*=4;
	
	// x=x/4;
	// x=x/=4;
	
	// x=x%4;
	// x%=4;
	
	printf("%d\n" ,x);
	
	return 0;
	
}
