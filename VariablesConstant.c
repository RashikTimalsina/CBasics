#include <stdio.h>

#define PI 3.14  //it is fix now we cannot change it;s value later on

/*
Variables: Something that stores value;
            Cannot start with number, symbol.

Constant: Once declare cannot be changed

Datatype var_name= value;

*/

int main(){
	
      int firstNumber =100;        	
      printf("The first number is %d\n", firstNumber);
	  printf("The value of PI is %.2f\n", PI);
	return 0;
}
